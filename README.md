# LatestFileSync
This script deletes all files (but not subfolders, unless specified) inside a given target directory.  Finds the most recently created file in a given source directory.  Copies that latest file from the source directory to the target directory.
